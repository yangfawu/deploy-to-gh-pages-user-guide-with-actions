import Section from "../components/Section"
import { Typography } from "@mui/material"

export default function Deploying() {
    return (
        <Section title="Deploying">
            <Typography>
                We are now ready to deploy. In your project, add the Git remote to your repository, commit everything, and push your first commit.
            </Typography>
        </Section>
    )
}
